<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/adminPartials/doctype.ejs') %>
        <title>
            <%= mytitle %>
        </title>
        <link rel="stylesheet" href="/style_admin/village.css">
        <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        // เช็คข้อความที่ส่งมาจาก URL
        const urlParams = new URLSearchParams(window.location.search);
        const successMessage = urlParams.get('message');
        const errorMessage = urlParams.get('error');

        if (successMessage) {
            Swal.fire({
                toast: true,
                position: 'top-end',
                icon: 'success',
                title: successMessage,
                showConfirmButton: false,
                timer: 3000
            });
        }

        if (errorMessage) {
            Swal.fire({
                toast: true,
                position: 'top-end',
                icon: 'error',
                title: errorMessage,
                showConfirmButton: false,
                timer: 3000
            });
        }

    </script>


    <%- include('../partials/adminPartials/adminHeader.ejs') %>

        <%- include('../partials/adminPartials/adminSidebar.ejs') %>
            <!-- Main content -->
            <div class="content">
                <div class="content-header">
                    <h1>หมู่บ้าน ( <%= villages.length %>)</h1>
                    <div class="add-media" onclick="wasFormToggle()">เพิ่มหมู่บ้าน</div>
                </div>
                <div class="was-container">
                    <div class="table-con">
                        <table class="table-auto">
                            <thead>
                                <tr>
                                    <th class="table-was-number">หมู่ที่</th>
                                    <th class="table-was-name">ชื่อหมู่บ้าน</th>
                                    <th class="table-was-location">ตำแหน่ง</th>
                                    <th class="table-was-delete">จัดการ</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% if (villages.length > 0) { %>
                                    <% villages.forEach((village, index) => { %>
                                        <tr>
                                            <td class="table-was-number"><%= village.villageNumber %></td>
                                            <td class="table-was-name"><%= village.villageName %></td>
                                            <td class="table-was-location"><%= village.location %></td>
                                            <td class="table-was-delete">
                                                <button type="button" class="was-row-<%= index + 1 %> row-active edit-btn" 
                                                    onclick="setVillageForm('<%= index + 1 %>', '<%= village._id %>', '<%= village.villageNumber %>', '<%= village.villageName %>', '<%= village.location %>')">                                                
                                                    <i class="fa fa-edit"></i>
                                                </button>
                                                <button type="button" class="delete-btn" 
                                                    onclick="deleteVillage(['<%= index + 1 %>', '<%= village._id %>', '<%= village.villageNumber %>', '<%= village.villageName %>', '<%= village.location %>'])">
                                                    <i class="fa fa-trash"></i>
                                                </button>
                                            </td>
                                        </tr>
                                    <% }) %>
                                <% } else { %>
                                    <tr>
                                        <td colspan="4" class="text-center py-2 px-4">ไม่พบข้อมูลหมู่บ้าน</td>
                                    </tr>
                                <% } %>
                            </tbody>
                        </table>
                    </div>

                    <!-- เพิ่มหมู่บ้าน -->
                    <div class="was-form" id="add_Form" style="display: none;">
                        <form action="/admin/villagePost" method="POST" enctype="application/x-www-form-urlencoded">
                            <div class="form-head">
                                <ion-icon class="head-logo" name="information-circle-outline"></ion-icon>
                                <span>เพิ่มหมู่บ้าน</span>
                            </div>
                            <div class="input-group">
                                <label for="villageNumber">หมู่ที่</label>
                                <input type="text" name="villageNumber" id="villageNumber" placeholder="เช่น 1, 2, 3, ..." required>
                    
                                <label for="villageName">ชื่อหมู่บ้าน</label>
                                <input type="text" name="villageName" id="villageName" placeholder="เช่น บ้านหนองแวง" required>
                    
                                <label for="location">ตำแหน่ง</label>
                                <input type="text" name="location" id="location" placeholder="เช่น ตำบล xxx อำเภอ xxx" required>
                    
                                <div class="submit-btn flex justify-end">
                                    <button type="button" class="cancel" onclick="cancelForm()">ยกเลิก</button>
                                    <button type="submit" class="submit">บันทึก</button>
                                </div>
                            </div>
                        </form>
                    </div>
                    
                    <!-- แก้ไขหมู่บ้าน -->
                    <div class="was-form" id="edit_Form" style="display: none;">
                        <form action="/admin/villageEdit" method="POST" enctype="application/x-www-form-urlencoded">
                            <div class="form-head">
                                <ion-icon class="head-logo" name="create-outline"></ion-icon>
                                <span>แก้ไขหมู่บ้าน</span>
                            </div>
                            <div class="input-group">
                                <!-- ส่งค่า _id -->
                                <input type="hidden" name="_id" id="hiddenVillageId" value="">

                                <label for="editvillageNumber">หมู่ที่</label>
                                <input type="text" name="villageNumber" id="editvillageNumber" placeholder="เช่น 1, 2, 3, ..." required>

                                <label for="editvillageName">ชื่อหมู่บ้าน</label>
                                <input type="text" name="villageName" id="editvillageName" placeholder="เช่น บ้านหนองแวง" required>

                                <label for="editlocation">ตำแหน่ง</label>
                                <input type="text" name="location" id="editlocation" placeholder="เช่น ตำบล xxx อำเภอ xxx" required>

                                <div class="submit-btn flex justify-end">
                                    <button type="button" class="cancel" onclick="cancelForm()">ยกเลิก</button>
                                    <button type="submit" class="submit">บันทึก</button>
                                </div>
                            </div>
                        </form>
                    </div>                    
                </div>
            </div>
</body>
<%- include('../partials/adminPartials/adminFooter.ejs') %>

    <script>
        let lastClickedRow = null;

        function setVillageForm(Index, Id, Number, Name, location) {
            const editForm = document.getElementById('edit_Form');
            const addForm = document.getElementById('add_Form');

            const InputVillageId = document.getElementById('hiddenVillageId');
            InputVillageId.value = Id;

            const InputVillageNumber = document.getElementById('editvillageNumber');
            InputVillageNumber.value = Number;

            const InputVillageName = document.getElementById('editvillageName');
            InputVillageName.value = Name;

            const InputVillagelocation = document.getElementById('editlocation');
            InputVillagelocation.value = location;

            addForm.style.display = 'none';
            const selectedRow = document.querySelector(`.was-row-${Index}`);
            if (lastClickedRow === selectedRow) {
                selectedRow.style.backgroundColor = '';
                lastClickedRow = null;
                editForm.style.display = 'none';
            } else {
                if (lastClickedRow) lastClickedRow.style.backgroundColor = '';
                selectedRow.style.backgroundColor = '#8EDBA4';
                editForm.style.display = 'block';
                lastClickedRow = selectedRow;
            }
        }
        function wasFormToggle() {
            const editForm = document.getElementById('edit_Form');
            const addForm = document.getElementById('add_Form');

            editForm.style.display = 'none';
            if (addForm.style.display === 'block') {
                addForm.style.display = 'none';
            }
            else {
                addForm.style.display = 'block';
            }
        }
        function deleteVillage(data) {
            Swal.fire({
                title: `ลบ "${data[3]}" หรือไม่?`,
                text: "หากคุณลบแล้วจะไม่สามารถกู้คืนได้!",
                icon: "warning",
                showCancelButton: true,
                confirmButtonColor: "#3085d6",
                cancelButtonColor: "#d33",
                confirmButtonText: "ยืนยัน",
                cancelButtonText: "ยกเลิก"
            }).then((result) => {
                if (result.isConfirmed) {
                    fetch('/admin/villageDelete/' + data[1], {
                        method: 'DELETE',
                        headers: {
                            'Content-Type': 'application/json',
                        },
                    })
                        .then(result => {
                            Swal.fire('ลบข้อมูลสำเร็จ', 'ข้อมูลหมู่บ้านถูกลบเรียบร้อยแล้ว', 'success')
                            .then(() => {
                                location.reload();
                            });
                        })
                        .catch(error => {
                            Swal.fire('เกิดข้อผิดพลาด', 'ไม่สามารถเชื่อมต่อกับเซิร์ฟเวอร์ได้', 'error');
                        });
                }
            });
        }
        function cancelForm() {
            const editForm = document.getElementById('edit_Form');
            const addForm = document.getElementById('add_Form');

            // Hide both forms when cancel is clicked
            editForm.style.display = 'none';
            addForm.style.display = 'none';

            // Optionally reset the forms
            editForm.reset();
            addForm.reset();
        }
    </script>
</html>
<!DOCTYPE html>
<html lang="th">

<head>
    <%- include('../partials/employeePartials/doctype.ejs') %>
    <title><%= mytitle %></title>
    <link rel="stylesheet" href="/style_employee/complaint-requests.css">
    <link rel="stylesheet" href="/style_employee/res_req.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script type="module" src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.esm.js"></script>
    <script nomodule src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.js"></script>
</head>

<body>
    <%- include('../partials/employeePartials/empHeader.ejs') %>
    
    <script>
        // เช็คข้อความที่ส่งมาจาก URL
        const urlParams = new URLSearchParams(window.location.search);
        const successMessage = urlParams.get('message');
        const errorMessage = urlParams.get('error');

        if (successMessage) {
            Swal.fire({
                icon: 'success',
                title: 'สำเร็จ!',
                text: successMessage,
                confirmButtonText: 'ตกลง',
                confirmButtonColor: '#28a745',
                timer: 3000
            });
        }

        if (errorMessage) {
            Swal.fire({
                toast: true,
                position: 'top-end',
                icon: 'error',
                title: errorMessage,
                showConfirmButton: false,
                timer: 3000
            });
        }
    </script>

    <div class="flex w-full">
        <%- include('../partials/employeePartials/empSidebar.ejs') %>
        <main class="content bg-gray-50 min-screen">
            <div class="mt-4 mx-4 mb-6 bg-white rounded-lg shadow p-4 flex justify-between items-center">
                <div class="flex flex-col md:flex-row justify-between items-start md:items-center">
                    <div>
                        <h1 class="text-2xl md:text-3xl font-bold text-gray-800 flex items-center">
                            รายการคำร้องหรือข้อร้องเรียน
                        </h1>
                        <p class="text-gray-600 mt-2">ระบบจัดการคำร้องและข้อร้องเรียนจากประชาชน</p>
                    </div>
                </div>
            </div>

            <div id="filter-container" class="mx-4 mb-6 bg-white rounded-xl shadow-sm border border-gray-100 p-5">
                <div class="flex flex-col lg:flex-row justify-between items-start lg:items-center gap-4">
                    <div class="flex flex-col sm:flex-row items-start sm:items-center gap-3 w-full lg:w-auto">
                        <span class="text-gray-700 font-medium whitespace-nowrap">สถานะ:</span>
                        <div class="flex flex-wrap gap-2">
                            <button id="filter-all" class="filter-btn px-4 py-1.5 rounded-full text-sm font-medium transition-all duration-200 shadow-sm hover:shadow bg-green-600 text-white active ring-2 ring-green-600 ring-offset-1">
                                ทั้งหมด (<%= complaint.length %>)
                            </button>
                            <button id="filter-pending" class="filter-btn px-4 py-1.5 rounded-full text-sm font-medium transition-all duration-200 shadow-sm hover:shadow bg-white text-gray-600 border border-gray-200 hover:bg-gray-50 hover:text-green-600">
                                รอดำเนินการ
                            </button>
                            <button id="filter-in-progress" class="filter-btn px-4 py-1.5 rounded-full text-sm font-medium transition-all duration-200 shadow-sm hover:shadow bg-white text-gray-600 border border-gray-200 hover:bg-gray-50 hover:text-green-600">
                                กำลังดำเนินการ
                            </button>
                            <button id="filter-resolved" class="filter-btn px-4 py-1.5 rounded-full text-sm font-medium transition-all duration-200 shadow-sm hover:shadow bg-white text-gray-600 border border-gray-200 hover:bg-gray-50 hover:text-green-600">
                                เสร็จสิ้น
                            </button>
                        </div>
                    </div>
                    
                    <div class="w-full lg:w-auto">
                        <div class="relative group">
                            <input type="text" id="search-input" placeholder="ค้นหารายการ..." class="w-full sm:w-80 pl-10 pr-4 py-2 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent transition-all shadow-sm">
                            <div class="absolute left-3 top-2.5 text-gray-400 group-focus-within:text-green-500 transition-colors">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                                </svg>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="mx-4 mb-6 bg-white rounded-lg shadow p-4">
                <div class="flex justify-between items-center mb-6 pb-4 border-b border-gray-200">
                    <h2 class="text-base font-semibold text-gray-800">
                        รายการทั้งหมด <span id="total-count" class="text-blue-600 text-base font-normal"><%= complaint.length %> รายการ</span>
                    </h2>
                    <div class="flex items-center space-x-2">
                        <span class="text-gray-600">เรียงตาม:</span>
                        <select id="sort-select" class="px-3 py-2 border border-gray-300 rounded-md text-gray-700 focus:outline-none focus:ring-2 focus:ring-blue-500">
                            <option value="newest">ล่าสุด</option>
                            <option value="oldest">เก่าสุด</option>
                            <option value="status">สถานะ</option>
                        </select>
                    </div>
                </div>

                <div id="complaints-container" class="space-y-4">
                    <% if (complaint.length > 0) { %>
                        <% complaint.forEach(com => { %>
                            <div class="group bg-white rounded-xl border border-gray-200 shadow-sm hover:shadow-md transition-all duration-300 overflow-hidden complaint-item" 
                                data-status="<%= com.status %>" 
                                data-id="<%= com._id %>" 
                                data-family="<%= com.family.familyName %>">
                                
                                <!-- Header: User Info & Status -->
                                <div class="px-5 py-4 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 border-b border-gray-100 bg-gray-50/50">
                                    <div class="flex items-center gap-3">
                                        <div class="w-10 h-10 rounded-full bg-red-100 flex items-center justify-center text-red-600">
                                            <i class="fas fa-user text-lg"></i>
                                        </div>
                                        <div>
                                            <h3 class="text-base font-semibold text-gray-900"><%= com.family.familyName %></h3>
                                            <p class="text-xs text-gray-500">รหัสคำร้อง: #<%= com._id.toString().slice(-6) %></p>
                                        </div>
                                    </div>
                                    <div>
                                        <% if (com.status === 'pending') { %>
                                            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800">
                                                <span class="w-1.5 h-1.5 bg-yellow-500 rounded-full mr-1.5"></span>รอดำเนินการ
                                            </span>
                                        <% } else if (com.status === 'in-progress') { %>
                                            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                                                <span class="w-1.5 h-1.5 bg-blue-500 rounded-full mr-1.5"></span>กำลังดำเนินการ
                                            </span>
                                        <% } else if (com.status === 'resolved') { %>
                                            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                                <span class="w-1.5 h-1.5 bg-green-500 rounded-full mr-1.5"></span>เสร็จสิ้น
                                            </span>
                                        <% } %>
                                    </div>
                                </div>
                                
                                <!-- Body: Details -->
                                <div class="p-5 grid grid-cols-1 md:grid-cols-12 gap-6">
                                    <!-- Details Column -->
                                    <div class="md:col-span-9 space-y-3">
                                        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                                            <div class="flex items-start gap-3">
                                                <div class="mt-0.5 text-gray-400">
                                                    <i class="far fa-calendar-alt"></i>
                                                </div>
                                                <div>
                                                    <p class="text-xs font-medium text-gray-500 uppercase tracking-wide">วันที่แจ้ง</p>
                                                    <p class="text-sm text-gray-900 font-medium whitespace-nowrap">
                                                        <%= com.createdAt.toLocaleString('th-TH', { dateStyle: 'medium', timeStyle: 'short', timeZone: 'Asia/Bangkok' }) %>
                                                    </p>
                                                </div>
                                            </div>
                                            <div class="flex items-start gap-3">
                                                <div class="mt-0.5 text-gray-400">
                                                    <i class="fas fa-tag"></i>
                                                </div>
                                                <div>
                                                    <p class="text-xs font-medium text-gray-500 uppercase tracking-wide">หมวดหมู่</p>
                                                    <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-gray-100 text-gray-800">
                                                        <% if (com.category === 'waste') { %>
                                                            การจัดการขยะ
                                                        <% } else if (com.category === 'service') { %>
                                                            การบริการของเจ้าหน้าที่
                                                        <% } else if (com.category === 'noise') { %>
                                                            เสียงรบกวน
                                                        <% } else if (com.category === 'sale') { %>
                                                            การขายขยะ
                                                        <% } else { %>
                                                            อื่นๆ
                                                        <% } %>
                                                    </span>
                                                </div>
                                            </div>
                                        </div>
                                        
                                        <% if (com.image) { %>
                                            <div class="mt-3">
                                                <p class="text-xs font-medium text-gray-500 uppercase tracking-wide mb-1 flex items-center gap-1">
                                                    <i class="fas fa-image text-gray-400"></i> มีรูปภาพประกอบ
                                                </p>
                                            </div>
                                        <% } %>
                                    </div>
                                    
                                    <!-- Actions Column -->
                                    <div class="md:col-span-3 flex md:flex-col justify-end md:justify-center gap-3 border-t md:border-t-0 md:border-l border-gray-100 pt-4 md:pt-0 md:pl-4">
                                        <button onclick="conplaintReply('<%= com._id %>')" 
                                                class="flex-1 md:flex-none inline-flex justify-center items-center px-4 py-2 bg-green-600 hover:bg-green-700 text-white text-sm font-medium rounded-lg transition-colors shadow-sm hover:shadow">
                                            <i class="fa-solid fa-reply mr-2"></i> ตอบกลับ
                                        </button>
                                        <button onclick="openModal('<%= com._id %>')" 
                                                class="flex-1 md:flex-none inline-flex justify-center items-center px-4 py-2 bg-white border border-gray-300 hover:bg-gray-50 text-gray-700 text-sm font-medium rounded-lg transition-colors">
                                            <i class="fa-solid fa-circle-info mr-2 text-gray-500"></i> รายละเอียด
                                        </button>
                                    </div>
                                </div>
                            </div>
                            
                            <!-- Modal รายละเอียดคำร้อง -->
                            <div id="modal-<%= com._id %>" class="hidden fixed inset-0 z-[60] bg-black bg-opacity-30">
                                <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-full max-w-2xl px-4 max-h-[90vh] overflow-y-auto">
                                    <div class="bg-white rounded-xl shadow-lg p-8 space-y-6">
                                        
                                        <div class="flex items-center justify-between border-b pb-4">
                                            <div class="flex items-center space-x-3">
                                                <ion-icon name="document-text-outline" class="text-blue-500 text-2xl"></ion-icon>
                                                <span class="text-xl font-semibold text-gray-800">รายละเอียดคำร้อง</span>
                                            </div>
                                            <button type="button" onclick="closeModal('<%= com._id %>')" class="text-gray-400 hover:text-gray-600 transition">
                                                <ion-icon name="close-outline" class="text-2xl"></ion-icon>
                                            </button>
                                        </div>

                                        <div class="space-y-5">
                                            <div>
                                                <label class="block text-sm font-medium text-gray-700 mb-2">ผู้แจ้งคำร้อง</label>
                                                <div class="flex items-center space-x-2 px-4 py-3 bg-gray-50 rounded-lg border border-gray-200">
                                                    <!-- <ion-icon name="person-outline" class="text-gray-600 text-xl"></ion-icon> -->
                                                    <span class="text-gray-800 font-medium"><%= com.family.familyName %></span>
                                                </div>
                                            </div>

                                            <div>
                                                <label class="block text-sm font-medium text-gray-700 mb-2">วันที่แจ้งคำร้อง</label>
                                                <div class="flex items-center space-x-2 px-4 py-3 bg-gray-50 rounded-lg border border-gray-200">
                                                    <!-- <ion-icon name="calendar-outline" class="text-blue-500 text-xl"></ion-icon> -->
                                                    <span class="text-gray-800"><%= com.createdAt.toLocaleString('th-TH', { dateStyle: 'full', timeStyle: 'medium', timeZone: 'Asia/Bangkok' }) %></span>
                                                </div>
                                            </div>

                                            <div>
                                                <label class="block text-sm font-medium text-gray-700 mb-2">หมวดหมู่</label>
                                                <div class="px-4 py-3 bg-gray-50 rounded-lg border border-gray-200">
                                                    <span class="text-gray-800">
                                                        <% if (com.category === 'waste') { %>
                                                            การจัดการขยะ
                                                        <% } else if (com.category === 'service') { %>
                                                            การบริการของเจ้าหน้าที่
                                                        <% } else if (com.category === 'noise') { %>
                                                            เสียงรบกวน
                                                        <% } else if (com.category === 'sale') { %>
                                                            การขายขยะ
                                                        <% } else { %>
                                                            อื่นๆ
                                                        <% } %>
                                                    </span>
                                                </div>
                                            </div>

                                            <!-- <div>
                                                <label class="block text-sm font-medium text-gray-700 mb-2">สถานะ</label>
                                                <div>
                                                    <span id="modal-status-<%= com._id %>" class="inline-flex items-center px-4 py-2 rounded-lg text-sm font-medium 
                                                        <% if (com.status === 'pending') { %>
                                                            bg-yellow-100 text-yellow-800
                                                        <% } else if (com.status === 'in-progress') { %>
                                                            bg-blue-100 text-blue-800
                                                        <% } else if (com.status === 'resolved') { %>
                                                            bg-green-100 text-green-800
                                                        <% } %>
                                                    ">
                                                        <% if (com.status === 'pending') { %>
                                                            รอดำเนินการ
                                                        <% } else if (com.status === 'in-progress') { %>
                                                            กำลังดำเนินการ
                                                        <% } else if (com.status === 'resolved') { %>
                                                            เสร็จสิ้น
                                                        <% } %>
                                                    </span>
                                                </div>
                                            </div> -->

                                            <div>
                                                <label class="block text-sm font-medium text-gray-700 mb-2">รายละเอียด</label>
                                                <div class="px-4 py-3 bg-gray-50 rounded-lg border border-gray-200">
                                                    <p class="text-gray-800 whitespace-pre-line leading-relaxed"><%= com.complaintMessage %></p>
                                                </div>
                                            </div>

                                            <div>
                                                <label class="block text-sm font-medium text-gray-700 mb-2">รูปภาพประกอบ</label>
                                                <% if (com.image) { %>
                                                    <div class="flex justify-center p-4 bg-gray-50 rounded-lg border border-gray-200">
                                                        <img src="<%= com.image %>" alt="รูปภาพประกอบ" class="max-w-full h-auto rounded-lg shadow-sm">
                                                    </div>
                                                <% } else { %>
                                                    <div class="px-4 py-6 bg-gray-50 rounded-lg border border-gray-200 text-center">
                                                        <ion-icon name="image-outline" class="text-gray-400 text-4xl mx-auto mb-2"></ion-icon>
                                                        <p class="text-gray-400 text-sm italic">ไม่มีรูปภาพประกอบ</p>
                                                    </div>
                                                <% } %>
                                            </div>
                                        </div>

                                        <div class="flex justify-end pt-4">
                                            <button type="button" onclick="closeModal('<%= com._id %>')" 
                                                class="px-6 py-2.5 bg-gray-200 hover:bg-gray-300 text-gray-800 font-medium rounded-lg transition duration-200">
                                                ปิด
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <div class="flex flex-col items-center justify-center py-12 bg-gray-50 rounded-lg border border-gray-200">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16 text-gray-300 mb-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                            </svg>
                            <h3 class="text-xl font-medium text-gray-700">ไม่พบรายการคำร้องหรือข้อร้องเรียน</h3>
                            <p class="text-gray-500 mt-2 text-center">ยังไม่มีคำร้องหรือข้อร้องเรียนในระบบ</p>
                        </div>
                    <% } %>
                </div>
            </div>
        </main>  
    </div>
    
    <%- include('../partials/employeePartials/empFooter.ejs') %>
    
    <script>
        // Modal functions
        function openModal(id) {
            document.getElementById(`modal-${id}`).classList.remove('hidden');
            document.body.style.overflow = 'hidden';
        }
        
        function closeModal(id) {
            document.getElementById(`modal-${id}`).classList.add('hidden');
            document.body.style.overflow = 'auto';
        }
        
        // Close modal when clicking outside
        document.addEventListener('click', function(event) {
            const modals = document.querySelectorAll('[id^="modal-"]');
            modals.forEach(modal => {
                if (event.target === modal) {
                    const id = modal.id.replace('modal-', '');
                    closeModal(id);
                }
            });
        });

        // Dropdown menu functions
        function toggleStatusMenu(id) {
            const menu = document.getElementById('statusMenu-' + id);
            // ปิด dropdown อื่นๆ ที่เปิดอยู่
            document.querySelectorAll('[id^="statusMenu-"]').forEach(m => {
                if (m.id !== 'statusMenu-' + id) {
                    m.classList.add('hidden');
                }
            });
            // Toggle dropdown ที่คลิก
            menu.classList.toggle('hidden');
        }

        // Update status function
        async function updateStatus(id, status) {
            try {
                // ปิด dropdown หลังจากเลือก
                const menu = document.getElementById('statusMenu-' + id);
                if (menu) {
                    menu.classList.add('hidden');
                }

                const response = await fetch(`/employee/complaint/${id}/status`, {
                    method: 'PATCH',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ status })
                });

                const result = await response.json();

                if (result.success) {
                    // Update status badge in the list
                    const statusBadge = document.querySelector(`[data-id="${id}"] .status-badge`);
                    const modalStatus = document.getElementById(`modal-status-${id}`);
                    const modalSelect = document.getElementById(`modal-status-select-${id}`);
                    
                    // Update dropdown button
                    const statusBtn = document.getElementById(`statusBtn-${id}`);
                    
                    // Update badge classes and text
                    statusBadge.className = 'status-badge ml-3 px-2 py-1 text-xs font-medium rounded-full ';
                    modalStatus.className = 'inline-flex items-center px-3 py-1 rounded-full text-sm font-medium ';
                    
                    let statusText = '';
                    let btnContent = '';
                    if (status === 'pending') {
                        statusBadge.className += 'bg-yellow-100 text-yellow-800';
                        modalStatus.className += 'bg-yellow-100 text-yellow-800';
                        statusText = 'รอดำเนินการ';
                        btnContent = '<i class="fas fa-clock text-yellow-300 mr-2"></i>รอดำเนินการ<i class="fas fa-chevron-down ml-2"></i>';
                    } else if (status === 'in-progress') {
                        statusBadge.className += 'bg-blue-100 text-blue-800';
                        modalStatus.className += 'bg-blue-100 text-blue-800';
                        statusText = 'กำลังดำเนินการ';
                        btnContent = '<i class="fas fa-spinner text-blue-300 mr-2"></i>กำลังดำเนินการ<i class="fas fa-chevron-down ml-2"></i>';
                    } else if (status === 'resolved') {
                        statusBadge.className += 'bg-green-100 text-green-800';
                        modalStatus.className += 'bg-green-100 text-green-800';
                        statusText = 'เสร็จสิ้น';
                        btnContent = '<i class="fas fa-check text-green-300 mr-2"></i>เสร็จสิ้น<i class="fas fa-chevron-down ml-2"></i>';
                    }
                    
                    statusBadge.textContent = statusText;
                    modalStatus.textContent = statusText;
                    
                    // Update dropdown button content
                    if (statusBtn) {
                        statusBtn.innerHTML = btnContent;
                    }
                    
                    // Update modal select
                    if (modalSelect) {
                        modalSelect.value = status;
                    }

                    // Update data attribute for filtering
                    const complaintItem = document.querySelector(`[data-id="${id}"]`);
                    complaintItem.setAttribute('data-status', status);

                    // Update dropdown menu highlights
                    const menuButtons = document.querySelectorAll(`#statusMenu-${id} button`);
                    menuButtons.forEach(btn => {
                        btn.classList.remove('bg-blue-50', 'text-blue-700');
                        btn.classList.add('text-gray-700');
                        // Remove check mark
                        const checkIcon = btn.querySelector('.fa-check.float-right');
                        if (checkIcon) {
                            checkIcon.remove();
                        }
                    });
                    
                    // Highlight selected status
                    const selectedBtn = document.querySelector(`#statusMenu-${id} button[onclick*="'${status}'"]`);
                    if (selectedBtn) {
                        selectedBtn.classList.remove('text-gray-700');
                        selectedBtn.classList.add('bg-blue-50', 'text-blue-700');
                        // Add check mark
                        selectedBtn.innerHTML += '<i class="fas fa-check text-blue-500 ml-auto float-right mt-0.5"></i>';
                    }

                    // แสดงข้อความสำเร็จ
                    Swal.fire({
                        icon: 'success',
                        title: 'สำเร็จ!',
                        text: result.message,
                        timer: 2000,
                        showConfirmButton: false
                    });
                } else {
                    throw new Error(result.error || 'เกิดข้อผิดพลาด');
                }
            } catch (error) {
                console.error('Error updating status:', error);
                Swal.fire({
                    toast: true,
                    position: 'top-end',
                    icon: 'error',
                    title: 'เกิดข้อผิดพลาดในการอัปเดตสถานะ',
                    showConfirmButton: false,
                    timer: 3000
                });
                
                // Revert select to original value
                location.reload();
            }
        }

        // Update status from modal
        function updateStatusFromModal(id, status) {
            updateStatus(id, status);
        }

        // Filter functionality
        document.addEventListener('DOMContentLoaded', function() {
            const filterButtons = document.querySelectorAll('.filter-btn');
            const complaintItems = document.querySelectorAll('.complaint-item');
            const totalCountElement = document.getElementById('total-count');
            const searchInput = document.getElementById('search-input');
            const sortSelect = document.getElementById('sort-select');

            // ปิด dropdown เมื่อคลิกนอกบริเวณ
            document.addEventListener('click', function(event) {
                if (!event.target.closest('[id^="statusBtn-"]') && !event.target.closest('[id^="statusMenu-"]')) {
                    document.querySelectorAll('[id^="statusMenu-"]').forEach(menu => {
                        menu.classList.add('hidden');
                    });
                }
            });

            // Filter by status
            filterButtons.forEach(button => {
                button.addEventListener('click', function() {
                    // Define class sets
                    const activeClasses = ['bg-green-600', 'text-white', 'ring-2', 'ring-green-600', 'ring-offset-1'];
                    const inactiveClasses = ['bg-white', 'text-gray-600', 'border', 'border-gray-200', 'hover:bg-gray-50', 'hover:text-green-600'];

                    // Update active button
                    filterButtons.forEach(btn => {
                        btn.classList.remove(...activeClasses);
                        btn.classList.add(...inactiveClasses);
                    });
                    this.classList.remove(...inactiveClasses);
                    this.classList.add(...activeClasses);

                    const filterId = this.id;
                    let visibleCount = 0;

                    complaintItems.forEach(item => {
                        const status = item.getAttribute('data-status');
                        let shouldShow = false;

                        if (filterId === 'filter-all') {
                            shouldShow = true;
                        } else if (filterId === 'filter-pending' && status === 'pending') {
                            shouldShow = true;
                        } else if (filterId === 'filter-in-progress' && status === 'in-progress') {
                            shouldShow = true;
                        } else if (filterId === 'filter-resolved' && status === 'resolved') {
                            shouldShow = true;
                        }

                        if (shouldShow) {
                            item.style.display = 'block';
                            visibleCount++;
                        } else {
                            item.style.display = 'none';
                        }
                    });

                    totalCountElement.textContent = `${visibleCount} รายการ`;
                });
            });

            // Search functionality
            searchInput.addEventListener('input', function() {
                const searchTerm = this.value.toLowerCase();
                let visibleCount = 0;

                complaintItems.forEach(item => {
                    const familyName = item.getAttribute('data-family').toLowerCase();
                    const complaintId = item.getAttribute('data-id');
                    
                    if (familyName.includes(searchTerm) || complaintId.includes(searchTerm)) {
                        // Only show if it also matches current filter
                        const currentFilter = document.querySelector('.filter-btn.bg-green-600');
                        let shouldShow = false;

                        // Default to show if no active filter found (shouldn't happen) or if 'all' is active
                        if (!currentFilter || currentFilter.id === 'filter-all') {
                            shouldShow = true;
                        } else {
                            const status = item.getAttribute('data-status');
                            if (currentFilter.id === 'filter-pending' && status === 'pending') {
                                shouldShow = true;
                            } else if (currentFilter.id === 'filter-in-progress' && status === 'in-progress') {
                                shouldShow = true;
                            } else if (currentFilter.id === 'filter-resolved' && status === 'resolved') {
                                shouldShow = true;
                            }
                        }

                        if (shouldShow) {
                            item.style.display = 'block';
                            visibleCount++;
                        } else {
                            item.style.display = 'none';
                        }
                    } else {
                        item.style.display = 'none';
                    }
                });

                totalCountElement.textContent = `${visibleCount} รายการ`;
            });

            // Sort functionality
            sortSelect.addEventListener('change', function() {
                const sortBy = this.value;
                const container = document.getElementById('complaints-container');
                const items = Array.from(complaintItems);

                items.sort((a, b) => {
                    if (sortBy === 'newest') {
                        // Sort by creation date (newest first) - using data-id as proxy since it contains timestamp
                        return b.getAttribute('data-id').localeCompare(a.getAttribute('data-id'));
                    } else if (sortBy === 'oldest') {
                        // Sort by creation date (oldest first)
                        return a.getAttribute('data-id').localeCompare(b.getAttribute('data-id'));
                    } else if (sortBy === 'status') {
                        // Sort by status: pending, in-progress, resolved
                        const statusOrder = { 'pending': 0, 'in-progress': 1, 'resolved': 2 };
                        const statusA = statusOrder[a.getAttribute('data-status')] || 999;
                        const statusB = statusOrder[b.getAttribute('data-status')] || 999;
                        return statusA - statusB;
                    }
                    return 0;
                });

                // Remove existing items and re-add in sorted order
                items.forEach(item => item.remove());
                items.forEach(item => container.appendChild(item));
            });
        });

        function conplaintReply(id) {                    
            window.location.href = `/employee/complaint/reply/${id}`;
            // Swal.fire({
            //     icon: 'info',
            //     title: 'แก้ไขจุดรับซื้อ',
            //     text: `กำลังไปยังหน้าแก้ไข ID: ${id}`,
            //     timer: 1500,
            //     showConfirmButton: false
            // });
        }
    </script>
</body>
</html>
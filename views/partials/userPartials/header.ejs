<style>
        .notification-bell-badge {
            animation: bellPulse 2s infinite;
        }
            @keyframes bellPulse {
            0% {
                box-shadow: 0 0 0 0 rgba(239, 68, 68, 0.7);
            }
            70% {
                box-shadow: 0 0 0 10px rgba(239, 68, 68, 0);
            }
            100% {
                box-shadow: 0 0 0 0 rgba(239, 68, 68, 0);
            }
        }
</style>

<!-- Header -->
<header class="bg-primary-green shadow-lg sticky top-0 z-50">
    <div class="container mx-auto px-4">
        <div class="flex items-center justify-between py-4">
            <!-- Logo -->
            <div class="flex items-center space-x-3">
                <a href="/"><img src="/img/logo_เปือยน้อย.png" class="w-[75px]" ></a>
                <!-- <div class="w-12 h-12 bg-white rounded-full flex items-center justify-center">
                        <span class="text-primary-green font-bold text-xl">
                        </span>
                    </div> -->
                <div class="text-white">
                    <h1 class="text-xl font-bold">กองทุนขยะรีไซเคิล</h1>
                    <p class="text-sm opacity-90">องค์การบริหารส่วนตำบลขามป้อม</p>
                </div>
            </div>

            <!-- Desktop Navigation -->
            <nav class="hidden lg:flex space-x-8 items-center">
                <a href="/"
                    class="text-white hover:text-light-green transition-colors font-medium">หน้าหลัก</a>  <!-- border-b-2 border-white -->
                <a href="/user/wastetype"
                    class="text-white hover:text-light-green transition-colors font-medium">ประเภทขยะ</a>
                <a href="/user/knowledge"
                    class="text-white hover:text-light-green transition-colors font-medium">สื่อความรู้</a>
                <a href="/user/complaint"
                    class="text-white hover:text-light-green transition-colors font-medium">คำร้อง/ข้อร้องเรียน</a>
                <a href="/user/wasteSaleRequest"
                    class="text-white hover:text-light-green transition-colors font-medium">แจ้งประสงค์ขายขยะ</a>
                <a href="/user/contact"
                    class="text-white hover:text-light-green transition-colors font-medium">ติดต่อสอบถาม</a>
            <% if (locals.session.username) { %>
                <div class="relative">
                    <button id="user-menu-button"
                        class="flex items-center gap-2 text-white bg-gray-800 hover:bg-gray-700 px-4 py-2 rounded-lg border border-gray-600 transition-all">
                        <a href="/"><img src="/img/logo_เปือยน้อย.png" class="w-[55px]"></a>
                        <span class="font-semibold">สวัสดี, <%= locals.session.username %></span>
                        <i class="fa-solid fa-chevron-down ml-1 text-sm transition-transform duration-200" id="dropdown-arrow"></i>
                    </button>
            
                    <!-- Dropdown -->
                    <div id="user-dropdown"
                        class="absolute right-0 mt-3 w-64 bg-stone-900 border border-stone-700 shadow-lg rounded-xl overflow-hidden opacity-0 scale-95 translate-y-[-10px] pointer-events-none transition-all duration-200 z-[50]">
            
                        <!-- ลูกศรเล็ก -->
                        <div class="absolute right-6 -top-2 w-4 h-4 bg-stone-900 border-l border-t border-stone-700 rotate-45">
                        </div>
            
                        <!-- Header -->
                        <div class="flex items-center gap-3 px-4 py-3 border-b border-stone-700">
                            <img src="/img/admin/logo.png" class="w-10 h-10 rounded-full">
                            <div class="text-sm text-white">
                                <div class="font-bold">
                                    <%= locals.session.username %>
                                </div>
                                <div class="text-gray-400 text-xs">ผู้ใช้งานระบบ</div>
                            </div>
                        </div>
            
                        <!-- Links -->
                        <a href="/user/profile"
                            class="flex items-center gap-2 px-4 py-2 text-green-400 hover:bg-green-800 hover:bg-opacity-30 transition-all border-b border-stone-800">
                            <i class="fa-solid fa-user"></i> โปรไฟล์
                        </a>
                        <a href="/user/notification"
                            class="flex items-center gap-2 px-4 py-2 text-green-400 hover:bg-green-800 hover:bg-opacity-30 transition-all border-b border-stone-800">
                            <i class="fa-solid fa-envelope"></i> กล่องข้อความ
                        </a>
                        <button id="logoutBtn"
                            class="w-full flex items-center gap-2 px-4 py-2 text-red-400 hover:bg-red-800 hover:bg-opacity-30 transition-all text-left">
                            <i class="fa-solid fa-right-from-bracket"></i> ออกจากระบบ
                        </button>
                    </div>
                </div>
                <% } else { %>
                    <a href="/login"
                        class="bg-white text-primary-green px-4 py-2 rounded-lg hover:bg-light-green transition-colors font-medium">เข้าสู่ระบบ</a>
                    <% } %>
            </nav>
            <!-- Mobile Menu Button -->
            <button id="mobile-menu-btn" class="lg:hidden text-white p-2">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16">
                    </path>
                </svg>
            </button>
        </div>

        <!-- Mobile Navigation -->
        <nav id="mobile-menu" class="lg:hidden hidden pb-4">
            <div class="flex flex-col space-y-2">
                <a href="/"
                    class="text-white hover:text-light-green transition-colors font-medium py-2 border-b border-green-400">หน้าหลัก</a>
                <a href="/user/wastetype"
                    class="text-white hover:text-light-green transition-colors font-medium py-2">ประเภทขยะ</a>
                <a href="/user/knowledge"
                    class="text-white hover:text-light-green transition-colors font-medium py-2">สื่อความรู้</a>
                <a href="/user/complaint"
                    class="text-white hover:text-light-green transition-colors font-medium py-2">คำข้อง/ข้อร้องเรียน</a>
                <a href="/user/wasteSaleRequest"
                    class="text-white hover:text-light-green transition-colors font-medium py-2">แจ้งประสงค์ขายขยะ</a>
                <a href="/user/contact"
                    class="text-white hover:text-light-green transition-colors font-medium py-2">ติดต่อสอบถาม</a>

                <% if (locals.session.username) { %>
                    <a href="/user/profile"
                        class="bg-white text-primary-green px-4 py-2 rounded-lg hover:bg-light-green transition-colors font-medium mt-2 text-center">โปรไฟล์</a>
                    <a href="/user/notification"
                        class="bg-white text-primary-green px-4 py-2 rounded-lg hover:bg-light-green transition-colors font-medium mt-2 text-center">กล่องข้อความ</a>
                    <a href="/logout"
                        class="bg-red-600 text-white px-4 py-2 rounded-lg hover:bg-red-500 transition-colors font-medium mt-2 text-center">ออกจากระบบ</a>
                    <% } else { %>
                    <a href="/login"
                        class="bg-white text-primary-green px-4 py-2 rounded-lg hover:bg-light-green transition-colors font-medium text-center">เข้าสู่ระบบ</a>
                <% } %>
            </div>
        </nav>
    </div>
</header>

    <!-- Chat Widget -->
    <div class="chat-widget">
        <button id="chat-button" class="chat-button">
            <i class="fas fa-comments"></i>
        </button>
        
        <div id="chat-window" class="chat-window">
            <div class="chat-header">
                <div class="chat-header-info">
                    <div class="chat-avatar">
                        <i class="fas fa-user-tie"></i>
                    </div>
                    <div class="chat-status">
                        <div class="chat-title">เจ้าหน้าที่ อบต.</div>
                        <div class="chat-subtitle">
                            <span class="status-dot"></span>
                            <span id="chat-status">พร้อมให้บริการ</span>
                        </div>
                    </div>
                </div>
                <button id="chat-close" class="chat-close">
                    <i class="fas fa-times"></i>
                </button>
            </div>
            
            <div id="chat-messages" class="chat-messages">
                <div class="chat-message bot">
                    <div class="message-bubble">
                        สวัสดีครับ! ยินดีต้อนรับสู่ อบต.ข้ามป้อม 🌿<br>
                        มีอะไรให้ช่วยเหลือไหมครับ?
                        <div class="message-time" id="welcome-time"></div>
                    </div>
                </div>
                <div class="quick-replies">
                    <div class="quick-reply" data-message="ขอทราบเวลาทำการ">⏰ เวลาทำการ</div>
                    <div class="quick-reply" data-message="ต้องการยื่นคำร้อง">📄 ยื่นคำร้อง</div>
                    <!-- <div class="quick-reply" data-message="สอบถามการชำระภาษี">💰 ชำระภาษี</div> -->
                    <div class="quick-reply" data-message="ขอข้อมูลติดต่อ">📞 ข้อมูลติดต่อ</div>
                </div>
                
                <div class="chat-message bot" style="margin-bottom: 0;">
                    <div class="typing-indicator" id="typing-indicator">
                        <div class="typing-dots">
                            <div class="typing-dot"></div>
                            <div class="typing-dot"></div>
                            <div class="typing-dot"></div>
                        </div>
                    </div>
                </div>
            </div>
            
            <div class="chat-input-container">
                <textarea id="chat-input" class="chat-input" placeholder="พิมพ์ข้อความ..." rows="1"></textarea>
                <button id="chat-send" class="chat-send">
                    <i class="fas fa-paper-plane"></i>
                </button>
            </div>
        </div>
    </div>
<!-- SweetAlert2 -->
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
document.addEventListener("DOMContentLoaded", () => {
  const userButton = document.getElementById("user-menu-button");
  const userDropdown = document.getElementById("user-dropdown");
  const arrow = document.getElementById("dropdown-arrow");

  if (userButton && userDropdown) {
    userButton.addEventListener("click", () => {
      const isOpen = !userDropdown.classList.contains("pointer-events-none");
      if (isOpen) {
        closeDropdown();
      } else {
        openDropdown();
      }
    });

    const openDropdown = () => {
      userDropdown.classList.remove("opacity-0", "scale-95", "translate-y-[-10px]", "pointer-events-none");
      userDropdown.classList.add("opacity-100", "scale-100", "translate-y-0");
      arrow.classList.add("rotate-180");
    };

    const closeDropdown = () => {
      userDropdown.classList.add("opacity-0", "scale-95", "translate-y-[-10px]", "pointer-events-none");
      userDropdown.classList.remove("opacity-100", "scale-100", "translate-y-0");
      arrow.classList.remove("rotate-180");
    };

    document.addEventListener("click", (event) => {
      if (!userButton.contains(event.target) && !userDropdown.contains(event.target)) {
        closeDropdown();
      }
    });
  }

  // Logout confirmation
  const logoutBtn = document.getElementById("logoutBtn");
  if (logoutBtn) {
    logoutBtn.addEventListener("click", (e) => {
      e.preventDefault();
      Swal.fire({
        title: "คุณต้องการออกจากระบบหรือไม่?",
        text: "หากออกจากระบบ คุณต้องเข้าสู่ระบบใหม่",
        icon: "warning",
        showCancelButton: true,
        confirmButtonColor: "#d33",
        cancelButtonColor: "#3085d6",
        confirmButtonText: "ออกจากระบบ",
        cancelButtonText: "ยกเลิก",
      }).then((result) => {
        if (result.isConfirmed) {
          window.location.href = "/logout";
        }
      });
    });
  }
});
</script>